import React from "react";
import { SafeAreaView } from "react-native";
import EcoFriendlyRouteFinder from "./EcoFriendlyRouteFinder";

const App = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <EcoFriendlyRouteFinder />
    </SafeAreaView>
  );
};

export default App;